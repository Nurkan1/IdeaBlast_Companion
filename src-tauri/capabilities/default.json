{
  "identifier": "default",
  "description": "Capability for the main window with dialog and URL opener access",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
