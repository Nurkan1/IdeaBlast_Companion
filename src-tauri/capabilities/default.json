{
  "identifier": "default",
  "description": "Capability for the main window with dialog and filesystem access",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default"
  ]
}
