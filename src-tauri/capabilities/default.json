{
  "identifier": "default",
  "description": "Capability for the main window with dialog, filesystem, and URL opener access",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    "opener:default"
  ]
}
